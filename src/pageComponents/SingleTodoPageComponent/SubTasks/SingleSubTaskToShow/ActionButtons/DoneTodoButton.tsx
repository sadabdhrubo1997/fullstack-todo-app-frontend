import styled from 'styled-components';

const DoneTodoButton = () => {
  return <ButtonTextWrapper>Mark as complete</ButtonTextWrapper>;
};

export default DoneTodoButton;
const ButtonTextWrapper = styled.div`
  display: block;
  cursor: pointer;
`;
